# Live OpenAI test fixture

Place an explicitly synthetic, non-customer `.pcm` file here for a separately
authorized live canary. The required format is signed PCM16LE, 24 kHz, mono. Audio
fixtures are intentionally ignored by Git.
